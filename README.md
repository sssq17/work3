## 程序交互方式说明​
添加控制点（鼠标左键）：通过鼠标左键在画布上点击以添加控制点。每次点击后，屏幕应记录并渲染出相应的控制点（如红色圆点）。​
观察曲线生成（实时渲染）：程序将自动用灰线连接所有的控制点形成控制多边形，并调用 GPU 实时绘制出对应的绿色贝塞尔曲线。此时可连续添加多个点，以观察曲线随控制多边形改变而发生的几何变化。​
清空画布（键盘 C 键）：当需要重置实验环境或绘制全新曲线时，通过按下键盘的 c 键，程序应清空所有已记录的控制点，并将画面恢复至初始的空白状态。
![1](0.gif)
